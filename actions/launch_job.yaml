---
name: launch_job
runner_type: python-script
description: "Launch a new job based on a job template"
enabled: true
entry_point: "launch_job.py"
parameters:
  job_template:
    type: "string"
    description: "Name of the job template to launch new job"
    required: true
